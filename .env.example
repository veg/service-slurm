# Slurm JWT Authentication Configuration
# ----------------------------------

# Path to the JWT key inside the container
# This is where the key will be placed (default location)
JWT_KEY_PATH=/var/spool/slurm/statesave/jwt_hs256.key

# Path to an external JWT key that will be copied to JWT_KEY_PATH
# Uncomment and set this to use your own key instead of generating one
# EXTERNAL_JWT_KEY_PATH=/jwt_keys/jwt_hs256.key

# Volume mount for the external JWT key
# Format: /path/to/local/key:[EXTERNAL_JWT_KEY_PATH]:ro
# Uncomment and set this when using an external key
# JWT_KEY_VOLUME=./keys/jwt_hs256.key:/jwt_keys/jwt_hs256.key:ro

# Note: To generate a compliant JWT key, run:
#   ./generate-jwt-key.sh
# This will create a key in the ./keys directory and provide
# instructions on how to use it with Docker.
